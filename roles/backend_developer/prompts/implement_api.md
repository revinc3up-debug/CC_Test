Implement the following API endpoints.

## Project Context
{{project_summary}}

## API Specification
{{api_spec}}

## Your Task
For each endpoint:
1. **Route definition** — Method, path, middleware chain.
2. **Request validation** — Schema validation for all inputs.
3. **Handler/Controller** — Request parsing, service call, response formatting.
4. **Error handling** — Proper HTTP status codes and error response format.
5. **Authentication/Authorization** — Guard middleware and permission checks.
6. **Tests** — Integration tests covering happy path, validation errors, auth, and edge cases.

Follow the project's existing patterns. Use dependency injection for services.
